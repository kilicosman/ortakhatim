

{
  "name": "ortakhatim",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "build": "mkdir -p public && cp -r ../*.html ../*.css ../*.js public/"
  },
  "dependencies": {
    "express": "^4.17.1",
    "dotenv": "^8.2.0",
    "@supabase/supabase-js": "^1.11.0"
  }
}
