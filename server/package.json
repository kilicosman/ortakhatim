{
  "name": "ortakhatim",
  "version": "1.0.0",
  "scripts": {
    "start": "node server/server.js",
    "build": "cd server && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.17.1",
    "dotenv": "^8.2.0",
    "@supabase/supabase-js": "^1.11.0"
  }
}
